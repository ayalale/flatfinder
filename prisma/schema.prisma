generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Apartment {
  id     Int    @id @default(autoincrement())
  title  String
  city   String
  price  Int
  rooms  Int
  image  String
}